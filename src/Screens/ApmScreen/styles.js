import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    userImage: {
        width: 110,
        height: 110,
        borderRadius: 55,
        marginBottom: 12,
    },
});

export default styles;
